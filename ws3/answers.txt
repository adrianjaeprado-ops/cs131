A1: ls -l > listing.txt && date >> listing.txt
A2: ls /nonexistent_dir 2> errors.txt
A3: ls /nonexistent_dir /etc > output.txt 2> errors.txt
A4: ls /nonexistent_dir /etc &> combined.txt
A5: Its showing the listing of /etc is visible because stdout prints to the terminal but stderr is discarded by /dev/null. Command /dev/null is acting as a device that immediately deletes any data written to it.
A6: sort fruits.txt | uniq
A7: sort fruits.txt | uniq -c | sort -nr
A8: grep -c "ERROR" server.log
A9: grep "ERROR" server.log > errors_only.txt
A10: grep -oE '^[A-Z]+' server.log | sort | uniq -c | sort -nr
A11: grep -o '"state":"[A-Z]\{2\}"' /data/yelp/yelp_academic_dataset_business.json | sort | uniq -c | sort -nr | head -n 5
B1: uid=1059(ishigaki-cs26) gid=1059(ishigaki-cs26) groups=1059(ishigaki-cs26),44(video),988(docker),993(render). A UID (User ID) is a unique number that identifies your specific user account on the system, while a GID (Group ID) identifies your primary group, which is used to manage file sharing permissions among multiple users.
B2: -rw-rw-r--. It is broken down into three sections: Owner (rw-) can read and write; Group (rw-) can read and write; Other (r--) can only read.
B3: My umask is 0002, which produces default permissions of 664 (rw-rw-r--) for new regular files by subtracting 002 from the maximum possible base system permissions of 666.
B4: chmod 000 secret.txt && chmod u+rw secret.txt
B5: chmod 640 fruits.txt
B6: find . -type f -user ishigaki-cs26 -perm /u+w
C1: bash long.sh > log.txt & (PID: 834317)
C2: Job number, Status, and Command.
C3: fg %1 followed by Ctrl-Z changes the job state to Stopped.
